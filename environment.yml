name: grill-names
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.6
  - pip:
    - naming
    - "--editable=git+https://github.com/sphinx-doc/sphinx.git@stable"
    - sphinx_autodoc_typehints
    - sphinx_rtd_theme
