language: python
python:
  - "3.6"
#install:
#  - pip install -r requirements.txt
#  - pip install python-coveralls
#script:
#  - coverage run -m unittest discover $TRAVIS_BUILD_DIR -v
#after_success:
#  - coveralls

#before_install:
install:
  - wget http://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh
  - bash miniconda.sh -b -p $HOME/miniconda
  - export PATH="$HOME/miniconda/bin:$PATH"

  # Update conda and create test environment
  - conda config --set always_yes yes --set changeps1 no
#  - conda config --add channels conda-forge
  - conda update -q conda
  # Useful for debugging any issues with conda
  - conda info -a
  - conda create -q -n test-environment python=$TRAVIS_PYTHON_VERSION
  - source activate test-environment
  - pip install sphinx sphinx_rtd_theme sphinx_autodoc_typehints  # Install documentation dependencies
#  - pip install python-coveralls
  - python setup.py install

#install:
#  - pip install naming  # Required
#  - pip install .  # Test installation correctness
#  - pip install sphinx sphinx_rtd_theme sphinx_autodoc_typehints  # Install documentation dependencies
#  - python setup.py install

script:
#  - python setup.py test -vv  # Test against installed code
  - python -m unittest discover $TRAVIS_BUILD_DIR -v
#  - sphinx-build -vW -b html docs/source _HTMLTest #Test docs build
